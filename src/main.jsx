import React from "react";
import ReactDOM from "react-dom/client";
import MissionClearance from "../mission-clearance.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MissionClearance />
  </React.StrictMode>
);
